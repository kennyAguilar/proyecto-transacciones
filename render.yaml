services:
  - type: web
    name: proyecto-transacciones
    env: static
    buildCommand: npm run install:render && npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
